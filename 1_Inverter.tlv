\TLV
// Verilog is more explicit RTL, while TL-Verilog describes pipeline timing more compactly.
// In TL-Verilog, signals are written with a $ prefix.
// Keep exactly 3 spaces before logic statements; do not use Tab.
   $out = ! $in1;