\TLV
// && means AND, || means OR, ^ means XOR
// $in1 and $in2 are input signals. Outputs are generated from them.
   $and_gate = $in1 && $in2;
   $or_gate = $in1 || $in2;
   $xor_gate = $in1 ^ $in2;
